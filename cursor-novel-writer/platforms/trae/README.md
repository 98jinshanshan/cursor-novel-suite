# TRAE / TRAE CN / SOLO

Install:

```bash
npx skills add <owner>/cursor-novel-writer -a trae-cn -y
```

Project path: `.trae/skills/`  
Global (TRAE CN): `~/.trae-cn/skills/`

SOLO mode uses the same skill directory. Trigger via natural language or `#` skill match.

Import via Settings → Rules and Skills → Import File (SKILL.md).
