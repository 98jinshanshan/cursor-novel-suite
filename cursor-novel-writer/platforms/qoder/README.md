# Qoder

Install skills to `.qoder/skills/`:

```bash
npx skills add <owner>/cursor-novel-writer -a qoder -y
```

Or copy `skills/*` to `.qoder/skills/`.

Subagents: see [Qoder subagent docs](https://docs.qoder.com/extensions/subagent) —
bind skills `story-init`, `chapter-writing`, etc.
