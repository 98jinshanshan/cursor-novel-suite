# 工作区左侧目录（速查）

**权威架构规范：** [DIRECTORY-ARCHITECTURE.md](./DIRECTORY-ARCHITECTURE.md)（layout-version 2.0.0）

## 三句话

1. **只改** `cursor-novel-writer/skills/` 与 `cursor-novel-video/skills/`。
2. `.agents` / `.qoder` / `.trae` / `.cursor/skills` 是安装镜像，已 **整夹隐藏**；改源后运行 `platforms/install-skills.ps1`。
3. 可选：用根目录 **`novel-suite.code-workspace`** 打开，按 Writer / Docs / Novels 分栏。

## Phase 路径映射

见 [layout-phase-map.json](./layout-phase-map.json) 与 [docs/workflow/README.md](../workflow/README.md)。
